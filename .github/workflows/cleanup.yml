name: "Clean up old builds"
# on:
#   schedule:
#   - cron: "*/15 * * * *"
on:
  push:
    branches:
      - main
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Cleanup old builds
        run: git clone https://github.com/shippo-feron-sb/shippo-feron-sb.github.io.git
        run: cd shippo-feron-sb.github.io
        run: sh cleanup.sh
      