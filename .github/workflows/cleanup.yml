name: "Clean up old builds"
# on:
#   schedule:
#   - cron: "*/15 * * * *"
on:
  push:
    branches:
      - main
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Cleanup old builds
        run: ls
      